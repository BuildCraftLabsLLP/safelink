---
phase: 02-multi-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/i18n/en.json
  - data/i18n/hi.json
  - data/i18n/ta.json
  - data/i18n/te.json
  - data/i18n/bn.json
  - data/i18n/mr.json
  - data/i18n/kn.json
  - data/i18n/ml.json
  - data/i18n/gu.json
  - data/i18n/pa.json
  - data/names_i18n/hi.json
  - data/names_i18n/ta.json
  - data/names_i18n/te.json
  - data/names_i18n/bn.json
  - data/names_i18n/mr.json
  - data/names_i18n/kn.json
  - data/names_i18n/ml.json
  - data/names_i18n/gu.json
  - data/names_i18n/pa.json
autonomous: true

must_haves:
  truths:
    - "en.json contains every user-facing string from all 10 Jinja2 templates"
    - "Each language JSON has the same keys as en.json with translated values"
    - "State names are translated to local script for all 9 languages"
    - "Phone numbers in translation values remain ASCII digits"
    - "Acronyms (NDMA, NDRF, SEOC, SDRF, IMD, PM CARES) remain untranslated"
  artifacts:
    - path: "data/i18n/en.json"
      provides: "English source strings for all UI text"
      contains: "site_name"
    - path: "data/i18n/hi.json"
      provides: "Hindi UI translations"
      contains: "site_name"
    - path: "data/names_i18n/hi.json"
      provides: "Hindi state name translations"
      contains: "states"
  key_links:
    - from: "data/i18n/en.json"
      to: "data/i18n/{lang}.json"
      via: "identical key sets"
      pattern: "same JSON keys in every file"
---

<objective>
Create UI string translation files (en.json + 9 languages) and state name translation files (9 languages) for all translatable template strings and place names.

Purpose: Provide the translation data that the i18n build system (Plan 02-03) will consume to render pages in all 10 languages. UI strings cover every user-facing text in templates. State names let geographic navigation render in local scripts.

Output: 10 UI string JSON files in `data/i18n/`, 9 state name JSON files in `data/names_i18n/`
</objective>

<execution_context>
@/Users/rightfulguy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rightfulguy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-language/02-CONTEXT.md
@.planning/phases/02-multi-language/02-RESEARCH.md

Key source files to extract strings from:
@templates/base.html
@templates/home.html
@templates/state.html
@templates/district.html
@templates/city.html
@templates/guide.html
@templates/static_page.html
@templates/macros/emergency_bar.html
@templates/macros/breadcrumb.html
@templates/macros/language_switcher.html

State data for name translations:
@data/states.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract English UI strings and create en.json + 9 language UI translation files</name>
  <files>
    data/i18n/en.json
    data/i18n/hi.json
    data/i18n/ta.json
    data/i18n/te.json
    data/i18n/bn.json
    data/i18n/mr.json
    data/i18n/kn.json
    data/i18n/ml.json
    data/i18n/gu.json
    data/i18n/pa.json
  </files>
  <action>
    Create `data/i18n/` directory.

    **Step 1: Extract all user-facing strings from templates into `data/i18n/en.json`.**

    Audit every template file and extract ALL hardcoded English text into flat-key JSON. Use descriptive dot-notation-free keys (underscores only). The keys should be organized by template but stored flat. Expected ~85-100 keys covering:

    From `base.html`:
    - site_name: "SafeLink India"
    - tagline: "Emergency info for India — works on 2G"
    - skip_to_content: "Skip to content"
    - warning_text: "General information only."
    - read_disclaimer: "Read disclaimer"
    - emergency_label: "Emergency:"
    - footer_home: "Home"
    - footer_about: "About"
    - footer_disclaimer: "Disclaimer"
    - footer_firstaid: "First Aid"
    - footer_kit: "Emergency Kit"
    - page_title_suffix: "Emergency Info"

    From `emergency_bar.html`:
    - emergency_112: "Emergency"
    - police_label: "Police"
    - fire_label: "Fire"
    - ambulance_label: "Ambulance"
    - disaster_label: "Disaster"

    From `home.html`:
    - quick_reference: "Quick Reference"
    - earthquake_drop: "Earthquake:"
    - earthquake_action: "DROP, COVER, HOLD ON"
    - cyclone_action_brief: "Cyclone:"
    - cyclone_shelter: "Move to pucca shelter"
    - flood_action_brief: "Flood:"
    - flood_warning: "Do NOT wade through water"
    - fire_action_brief: "Fire:"
    - fire_action: "Get low, get out, call"
    - gas_leak_brief: "Gas leak:"
    - gas_leak_action: "Do NOT use electrical switches"
    - states_and_uts: "States & Union Territories"
    - disaster_safety_guides: "Disaster Safety Guides"
    - helplines_and_resources: "Helplines & Resources"
    - mental_health: "Mental Health"
    - other_helplines: "Other Helplines"
    - women_helpline: "Women Helpline"
    - child_helpline: "Child Helpline"
    - senior_citizen: "Senior Citizen"
    - government_resources: "Government Resources"
    - pm_cares_fund: "PM CARES Fund"

    From `state.html`:
    - state_emergency_numbers: "{state} Emergency Numbers"  (use "{state}" as placeholder)
    - disaster_helpline: "Disaster Helpline"
    - seoc_label: "SEOC"
    - seoc_mobile: "SEOC Mobile"
    - whatsapp_label: "WhatsApp"
    - toll_free: "Toll-free"
    - sdrf_label: "SDRF"
    - national_emergency: "National Emergency"
    - districts_heading: "Districts"
    - cities_heading: "Cities"
    - resources_heading: "Resources"
    - sdma_label: "{state} SDMA"
    - last_verified: "Last verified:"
    - back_to_state: "Back to {state}"

    From `district.html`:
    - emergency_numbers: "Emergency Numbers"
    - district_hq: "District HQ"

    From `city.html`:
    - population_rank: "Population rank: #{rank} in India"

    From `guide.html`:
    - emergency_actions: "Emergency Actions"
    - india_specific_notes: "India-Specific Notes"
    - other_guides: "Other Guides"

    From `static_page.html`: (no additional strings - uses page data)

    From `breadcrumb.html`:
    - home_breadcrumb: "Home"

    Also add a fallback string:
    - page_not_available: "This page is not yet available in {language}."

    **Step 2: Create translation files for all 9 languages.**

    For each language (hi, ta, te, bn, mr, kn, ml, gu, pa), create `data/i18n/{lang}.json` with the SAME keys as en.json, but values translated into that language's script.

    CRITICAL RULES for translations:
    1. ALL phone numbers must remain ASCII digits (0-9). Never convert to native script digits.
    2. Keep these acronyms untranslated: NDMA, NDRF, SEOC, SDRF, IMD, PM CARES, NDMA, SACHET
    3. Keep "SafeLink India" as the site name in all languages (do not transliterate).
    4. Keep URLs untranslated.
    5. Translate action instructions with emergency accuracy - these are life-safety instructions.
    6. "pucca" can remain as "pucca" or use the local equivalent term in each language.
    7. Use UTF-8 encoded characters directly in JSON (not HTML entities).
    8. Placeholder tokens like {state}, {rank}, {language} must remain as-is in translated strings.

    Each file should be valid JSON, same structure as en.json.
  </action>
  <verify>
    Run: `python3 -c "import json, os; files = [f for f in os.listdir('data/i18n') if f.endswith('.json')]; assert len(files) == 10, f'Expected 10 i18n files, got {len(files)}'; en = json.load(open('data/i18n/en.json')); en_keys = set(en.keys()); [json.load(open(f'data/i18n/{f}')) for f in files]; print(f'OK: {len(files)} files, {len(en_keys)} keys in en.json'); [print(f'  Key mismatch in {f}: missing={en_keys - set(json.load(open(f\"data/i18n/{f}\")).keys())}') for f in files if set(json.load(open(f'data/i18n/{f}')).keys()) != en_keys]"`
  </verify>
  <done>
    10 JSON files exist in data/i18n/ (en + 9 languages). All have identical key sets. en.json has 85+ keys covering every hardcoded string in all templates. All translation values use native scripts with ASCII digits for numbers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create state name translation files for all 9 languages</name>
  <files>
    data/names_i18n/hi.json
    data/names_i18n/ta.json
    data/names_i18n/te.json
    data/names_i18n/bn.json
    data/names_i18n/mr.json
    data/names_i18n/kn.json
    data/names_i18n/ml.json
    data/names_i18n/gu.json
    data/names_i18n/pa.json
  </files>
  <action>
    Create `data/names_i18n/` directory.

    For each of the 9 languages, create `data/names_i18n/{lang}.json` with this structure:

    ```json
    {
      "states": {
        "AP": "translated name in local script",
        "AR": "translated name in local script",
        ...all 36 state codes...
      }
    }
    ```

    The state codes are the `code` field from `data/states.json`. There are 36 entries:
    AP, AR, AS, BR, CG, GA, GJ, HR, HP, JH, KA, KL, MP, MH, MN, ML, MZ, NL, OD, PB, RJ, SK, TN, TS, TR, UP, UK, WB, AN, CH, DN, DD, DL, JK, LA, LD, PY

    Translation rules:
    1. Translate state/UT names to the local script of each language.
    2. For the language's home state, use the most commonly recognized local name (e.g., Tamil Nadu in Tamil = "தமிழ்நாடு").
    3. District and city names: include an empty "districts" and "cities" object for future use: `"districts": {}, "cities": {}`.
    4. Use UTF-8 characters directly (not HTML entities).
    5. Names should be the standard official transliteration used in government documents for that language.

    Validate each file is parseable JSON with all 36 state codes present.
  </action>
  <verify>
    Run: `python3 -c "import json, os; files = [f for f in os.listdir('data/names_i18n') if f.endswith('.json')]; assert len(files) == 9, f'Expected 9 name files, got {len(files)}'; [print(f'{f}: {len(json.load(open(f\"data/names_i18n/{f}\"))[\"states\"])} states') for f in sorted(files)]"`
  </verify>
  <done>
    9 JSON files in data/names_i18n/ (one per non-English language). Each has a "states" object with all 36 state/UT codes mapped to names in the target language's script. Each also has empty "districts" and "cities" objects.
  </done>
</task>

</tasks>

<verification>
1. `ls data/i18n/*.json | wc -l` returns 10
2. `ls data/names_i18n/*.json | wc -l` returns 9
3. All JSON files parse without error
4. en.json key count >= 85
5. Every language file has identical keys to en.json
6. Every names file has exactly 36 state entries
7. No Indic digits (U+0966-U+0D6F) appear in phone number values
</verification>

<success_criteria>
- 10 UI string files (en + hi + ta + te + bn + mr + kn + ml + gu + pa) with consistent keys
- 9 state name files with all 36 states translated to local script
- All files valid JSON, UTF-8 encoded
- Phone numbers remain ASCII, acronyms remain English
- Ready for build.py to consume via t() function in Plan 02-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-language/02-01-SUMMARY.md`
</output>
